<!
    strict;
    name: Regex;
    author: cailin;
    created: 2019-03-13;
    description: The file containing the type ms.lang.validators.ValidUrl;
>

/**
 * Meant to be tagged on strings that are intended to represent URLs.
 */
@{Target(value: FIELD, type: string)}
annotation ms.lang.validators.ValidUrl {
    
}