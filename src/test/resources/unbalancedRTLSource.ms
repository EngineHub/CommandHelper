@access_level = "user"
if(@access_level != 'none‮⁦'){ # Check if admin ⁩⁦' && @access_level != 'user
    console("You are an admin1.\n");
}

if(@access_level != 'none' && @access_level != 'user'){ # Check if admin
    console("You are an admin2");
}
